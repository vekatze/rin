data config {
| Config(
    host: &text,
    buffer-size: int,
    follow-location: bool,
  )
}

define default-config(host: &text): config {
  Config of {
    host,
    buffer-size = 1024,
    follow-location = True,
  }
}
